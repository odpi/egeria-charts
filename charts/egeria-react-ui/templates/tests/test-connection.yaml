apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "egeria-react-ui.fullname" . }}-test-connection"
  labels:
    {{- include "egeria-react-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "egeria-react-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
